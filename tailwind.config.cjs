const config = {
	content: ['./src/**/*.{html,js,ts,svelte}'],

	theme: {
		extend: {
			fontFamily: { inter: ['Inter', 'sans-serif'], 'open-sans': ['Open Sans', 'sans-serif'] }
		}
	},

	plugins: []
}

module.exports = config
